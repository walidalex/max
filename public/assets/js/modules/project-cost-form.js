'use strict';document.addEventListener('DOMContentLoaded',()=>{const select=document.getElementById('project-cost-code'),label=document.getElementById('project-cost-section');if(!select||!label)return;const update=()=>{label.textContent=select.selectedOptions[0]?.dataset.section?`قسم الأعمال: ${select.selectedOptions[0].dataset.section}`:'';};select.addEventListener('change',update);select.tomselect?.on('change',update);update();});
