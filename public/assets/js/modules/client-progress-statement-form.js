(()=>{'use strict';const form=document.querySelector('#cps-form');if(!form)return;const money=n=>(Math.round((Number(n)+Number.EPSILON)*100)/100).toFixed(2);const update=()=>{let cost=0,variation=0;document.querySelectorAll('.cps-cost:checked').forEach(x=>cost+=Number(x.dataset.amount||0));document.querySelectorAll('.cps-variation').forEach(x=>{const amount=Number(x.value||0);variation+=x.dataset.effect==='decrease'?-amount:amount;});const markup=Math.round((cost*Number(form.dataset.markup||0)/100+Number.EPSILON)*100)/100;const values={cost,markup,variation,total:cost+markup+variation};for(const[k,v]of Object.entries(values)){const el=document.querySelector('#sum-'+k);if(el)el.textContent=money(v);}};document.querySelector('#select-all-costs')?.addEventListener('click',()=>{document.querySelectorAll('.cps-cost').forEach(x=>x.checked=true);update();});form.addEventListener('input',update);form.addEventListener('change',update);update();})();
